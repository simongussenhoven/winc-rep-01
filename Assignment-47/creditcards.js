const getPersons = () => {
    //vind alle personen
    const allPersons = randomPersonData.map(person => {
        return person
    });
    
    allPersons.map(person => {
       country = [];

        
    })
}
getPersons()